# workload-simulator configuration

web:
    hostname:   localhost
    http_port:  8080
    public_dir: /go/src/github.com/yhat/workload-simulator/app/public
    views_dir: /go/src/github.com/yhat/workload-simulator/app/views
    report_dir: /root/workload_sim

settings:
    max_dial: 20
    max_workers: 30